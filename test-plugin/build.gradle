buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath "io.github.kayr:gradle-ezyquery-plugin:0.0.1"
    }
}

apply plugin: 'io.github.kayr.gradle.ezyquery'

ezyQuery{
    sqlScriptPath = "Silly.sql.path"
}